{
  "name": "Worker",
  "description": "Worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node service.js",
    "startLocal": "node service.js",
    "build": "tsc",
    "build:w": "tsc -w --noUnusedParameters --noUnusedLocals"
  },
  "dependencies": {
    "amqp": "^0.2.6"
  },
  "devDependencies": {
    "typescript": "^2.0.3"
  }
}
